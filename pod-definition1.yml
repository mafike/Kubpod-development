---
apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    author: mafike
spec:
  containers:
    - name: redis
      image: redis
      VolumeMounts:
        - name: redis-volume
          mountPath: /data/redis
  volumes:
    - name: redis-volume
      emptyDir: {}
